import 'package:flutter/material.dart';

/// A class that holds commonly used colors for the app.
///
/// Provides a centralized place to define theme colors like border, hint text, background, etc.
class AppColor {
  /// Default border color for text fields and containers.
  static const Color boderColor = Color(0xffECEAF0);

  /// Default hint text color for input fields.
  static const Color hintColor = Color(0xff5F5F5F);

// You can define more colors here as needed, e.g.,
// static const Color primaryColor = Color(0xff123456);
// static const Color background = Color(0xffffffff);
}
